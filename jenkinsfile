pipeline {

    agent { label 'test' }

    stages {
        stage('Pull on Test') {
            steps {
                git branch: 'develop',
                    url: 'https://github.com/janakdasari-art/website-new.git'
            }
        }
    }

    post {
        success {
            build job: 'prod-job'
        }
    }
}
