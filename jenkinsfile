pipeline {

        agent { label 'test'}

triggers {
 githubPush()
}

        stages{
        stage('pull when test'){
        steps{

        git branch : 'devlop',
                url : 'https://github.com/janakdasari-art/website-new.git'
        }
}
}
}
